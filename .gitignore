# Documento interno de procedencia (rutas absolutas de la máquina local) — no publicar
FUENTES.md

# Hipótesis workspace: investigación cerrada con resultado negativo (P1-P3
# refutadas, sin evidencia de que t=0 sea el workspace) — no publicar
evidence/WORKSPACE_HYPOTHESIS_REPORT.md

# Comparación interna contra póster conceptual externo (atribución a otro
# autor, rutas absolutas de la máquina local) — no publicar
evidence/informe_poster_y_lsgot.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Embeddings/trayectorias crudas — nunca deben terminar en este repo
# (v_identidad.npy es un vector derivado de 21KB, no un embedding crudo — no cae bajo *.npz)
*.npz

# Sistema
.DS_Store
Thumbs.db
*.swp

# Logs / caches de corridas
*.log
cache_*/
